# pico-artist-website-v2

Cloudflare Pages–ready Next.js app for the Piko Artist site. Built with Tailwind, shadcn/ui (Button, Card, Dialog), motion, Lenis, and a Wavesurfer-ready audio placeholder.

## Quick start
- Install: `npm install`
- Develop: `npm run dev`
- Build (OpenNext + output check): `npm run cf:build`

See `/docs/DEPLOY.md` for the single deploy command/output directory and `/docs/MIGRATION.md` for what to copy from the legacy repo after the first green deploy.
